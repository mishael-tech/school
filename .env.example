# MongoDB Atlas (or local) connection string
MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster.example.mongodb.net/school-scoreboard?retryWrites=true&w=majority

# JWT signing secret — use a long random string in production
JWT_SECRET=

# Cookie name defaults to admin_session in code; secure flag set automatically on Vercel
