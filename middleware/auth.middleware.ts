/**
 * Next.js only loads `middleware.ts` at the project root.
 * See `middleware.ts` in the repo root for the guard that protects `/admin/**`.
 * This file exists to match the documented project layout.
 */

export {};
